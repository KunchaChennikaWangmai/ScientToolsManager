<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toolsmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContactUsActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="ContactUs"></activity>
        <activity android:name=".ToolsListActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/menu_showProject"></activity>
        <activity android:name=".ProfilesListActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/menu_register"/>
        <activity android:name=".ShowTools"
            android:parentActivityName=".ToolsListActivity"
            android:label="Tool"/>
        <activity android:name=".ShowProfile"
            android:parentActivityName=".ProfilesListActivity"
            android:label="Profile"/>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>